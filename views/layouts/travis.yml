language: node_js

env:
  - DATABASE_URL=postgresql://postgres@localhost:5432/usersGreeted

node_js:  
  - '8.11.4'

before_script:
  - psql -c 'create database  usersGreeted;' -U postgres
  - psql -U postgres -d username -f table.sql

addons:
    apt:
    packages:
    - postgresql-server-dev-9.2